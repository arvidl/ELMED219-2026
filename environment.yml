name: elmed219-2026
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip

  # Core scientific computing
  - numpy>=1.24.0
  - pandas>=2.0.0
  - scipy>=1.10.0
  - matplotlib>=3.7.0
  - seaborn>=0.12.0

  # Jupyter ecosystem
  - jupyter>=1.0.0
  - jupyter-book>=0.15.0
  - ipywidgets>=8.0.0
  - nbconvert>=7.0.0

  # Machine learning
  - scikit-learn>=1.3.0
  - tensorflow>=2.13.0
  - pytorch>=2.0.0
  - torchvision>=0.15.0

  # Medical imaging
  - nibabel>=5.1.0
  - nilearn>=0.10.0
  - SimpleITK>=2.3.0
  - scikit-image>=0.21.0

  # Network analysis
  - networkx>=3.1.0

  # Utilities
  - tqdm>=4.65.0
  - requests>=2.31.0
  - pillow>=10.0.0

  # Install remaining packages via pip
  - pip:
      - plotly>=5.15.0
      - pydicom>=2.4.0
      - igraph>=0.10.0
      - keras>=2.13.0
      - tensorflow-addons>=0.21.0
      - gdown>=4.7.0
      - pyradiomics>=3.0.0
      - antspyx>=0.6.0
      - tokenizers>=0.20.0
    # Note: google-colab is only available in Colab environment
